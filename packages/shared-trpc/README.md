# shared-trpc

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-trpc` to build the library.

## Running unit tests

Run `nx test shared-trpc` to execute the unit tests via [Vitest](https://vitest.dev/).
